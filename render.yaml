services:
  - type: web
    name: Rahul-bot
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node bot.js"
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
    autoDeploy: true
